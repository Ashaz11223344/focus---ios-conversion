<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="20dp">

    <!-- Header Section -->
    <RelativeLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/widget_journal_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:fontFamily="@font/literata_bold"
            android:text="LATEST JOURNAL ENTRY"
            android:textColor="#FC6E20"
            android:textSize="11sp"
            android:textStyle="bold"
            android:letterSpacing="0.1" />

        <TextView
            android:id="@+id/widget_journal_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:fontFamily="@font/literata_medium"
            android:text="17 May 2026, 12:00"
            android:textColor="#9E9E9E"
            android:textSize="11sp" />
            
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_below="@id/widget_journal_title"
            android:layout_marginTop="8dp"
            android:src="@android:color/transparent"
            android:background="#323232" />
    </RelativeLayout>

    <!-- Journal Content Body -->
    <TextView
        android:id="@+id/widget_journal_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_header"
        android:layout_alignParentBottom="true"
        android:fontFamily="@font/literata_regular"
        android:gravity="top|start"
        android:ellipsize="end"
        android:maxLines="6"
        android:text="Start writing your journal"
        android:textColor="#FFE7D0"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.25" />

</RelativeLayout>
